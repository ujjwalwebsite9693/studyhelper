// Single source of truth for valid semesters on the backend — keep this in
// sync with frontend/src/constants/contentTypes.js SEMESTERS.
const VALID_SEMESTERS = [1, 2, 3, 4, 5, 6];

module.exports = { VALID_SEMESTERS };
